import React from "react";

const OperatorButton = () => {
  return (
    <>
      {/* Display a button element rendering the data being passed down from the parent container on props */}
    <h2>Operator Button Section</h2>
    </>
  );
};

export default OperatorButton